# Setup

To set up the Gesture Recognition System, follow these steps.

## Prerequisites
Before you start, make sure you have the following installed:

- Python 3.11
- Git (if cloning from GitHub)
- Required system dependencies for **tkinter** (if using `MouseInfo`).

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/yourusername/yourproject.git
   cd yourproject
